version: '3.8'

services:
  fitness-app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      # Mount data directory to persist workouts and tracking data
      - ./data:/app/data
    restart: unless-stopped
